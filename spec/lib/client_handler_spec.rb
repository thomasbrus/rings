require 'spec_helper'
require 'rings/client_handler'

describe ClientHandler, "with regard to ..." do
  let(:server) { double :server }
  let(:server) { double :client }
  let(:client_handler) { ClientHandler.new server, client }

  it "handles incoming commands correctly" do

  end
  
end